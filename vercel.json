{
  "version": 2,
  "buildCommand": "npm run build && mkdir -p public",
  "installCommand": "npm install --include=dev",
  "framework": null,
  "outputDirectory": "public",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "includeFiles": "{data/**,openapi/**,dist/ui/**,dist/fixtures/**}"
    }
  }
}

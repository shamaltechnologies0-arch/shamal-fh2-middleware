# Legacy document

**Marafiq is an example external viewer user/company — not the product identity.**

This document has moved to **[VIEWER_API_MATRIX.md](./VIEWER_API_MATRIX.md)**.

The `/v1/marafiq/*` API paths remain supported as legacy aliases during migration.
